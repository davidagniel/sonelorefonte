version: '3.8'

services:
  websonelo:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "12000:80"
    container_name: websonelo
    volumes:
      - ./Sonelo:/var/www/html
      - ./php.ini:/usr/local/etc/php/php.ini
    depends_on:
      - dbsonelo
    environment:
      - MYSQL_HOST=dbsonelo
      - MYSQL_USER=sonelo
      - MYSQL_PASSWORD=1#2*DFjvcftifgkE456
      - MYSQL_DATABASE=sonelo
    restart: always

  dbsonelo:
    image: mariadb:latest
    container_name: dbsonelo
    environment:
      MYSQL_ROOT_PASSWORD: 5jcDr5.6?qE7S*
      MYSQL_DATABASE: sonelo
      MYSQL_USER: sonelo
      MYSQL_PASSWORD: 1#2*DFjvcftifgkE456
       
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "12001:3306"
    restart: always

  phpmyadminsonelo:
    image: phpmyadmin:latest
    container_name: phpmyadminsonelo
    environment:
      PMA_HOST: dbsonelo
      #PMA_USER: sonelo
      #PMA_PASSWORD: 1#2*DFjvcftifgkE456
    ports:
      - "12002:80"
    depends_on:
      - dbsonelo
    restart: always

volumes:
  db_data:
